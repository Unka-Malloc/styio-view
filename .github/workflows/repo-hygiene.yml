name: repo-hygiene

on:
  pull_request:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hygiene:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    env:
      STYIO_PYTHON_VERSION: "3.13.5"

    steps:
      - name: Checkout Vityo nightly
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          path: vityo-nightly

      - name: Resolve sibling checkout ref
        id: sibling-ref
        shell: bash
        run: |
          requested="${GITHUB_REF_NAME}"
          if [[ "${GITHUB_EVENT_NAME}" == "pull_request" || "${GITHUB_EVENT_NAME}" == "pull_request_target" ]]; then
            requested="${{ github.base_ref }}"
          fi

          for ref in "$requested" nightly; do
            if git ls-remote --exit-code --heads https://github.com/Unka-Malloc/styio-nightly.git "$ref" >/dev/null 2>&1 \
              && git ls-remote --exit-code --heads https://github.com/Unka-Malloc/styio-spio.git "$ref" >/dev/null 2>&1; then
              echo "ref=$ref" >> "$GITHUB_OUTPUT"
              exit 0
            fi
          done

          echo "No common sibling checkout ref found for '$requested' or fallback 'nightly'." >&2
          exit 1

      - name: Check out styio-nightly sibling repository
        uses: actions/checkout@v5
        with:
          repository: Unka-Malloc/styio-nightly
          ref: ${{ steps.sibling-ref.outputs.ref }}
          path: styio-nightly

      - name: Check out styio-spio sibling repository
        uses: actions/checkout@v5
        with:
          repository: Unka-Malloc/styio-spio
          ref: ${{ steps.sibling-ref.outputs.ref }}
          path: styio-spio

      - name: Set up standardized Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.STYIO_PYTHON_VERSION }}

      - name: Check tracked tree
        working-directory: vityo-nightly
        run: python3 scripts/repo-hygiene-gate.py --mode tracked

      - name: Dependency policy gate
        working-directory: vityo-nightly
        run: python3 scripts/dependency-policy-gate.py

      - name: Supply chain governance gate
        working-directory: vityo-nightly
        run: python3 scripts/supply-chain-governance-gate.py

      - name: GitHub Actions pin gate (audit)
        working-directory: vityo-nightly
        run: python3 scripts/github-actions-pin-gate.py --mode audit

      - name: Architecture boundary gate
        working-directory: vityo-nightly
        run: python3 scripts/check_architecture_boundaries.py

      - name: Compatibility facade gate
        working-directory: vityo-nightly
        run: python3 scripts/check_compat_facades.py

      - name: Security baseline gate
        working-directory: vityo-nightly
        run: python3 scripts/check_security_baseline.py

      - name: Performance budget gate
        working-directory: vityo-nightly
        run: python3 scripts/check_performance_budgets.py

      - name: License policy gate
        working-directory: vityo-nightly
        run: python3 scripts/check_license_policy.py

      - name: Import boundary gate
        working-directory: vityo-nightly
        run: python3 scripts/import-boundary-gate.py

      - name: Check ecosystem CLI doc consistency
        working-directory: vityo-nightly
        run: python3 scripts/ecosystem-cli-doc-gate.py --require-workspace --json --non-blocking

      - name: Check incoming history range
        shell: bash
        working-directory: vityo-nightly
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            RANGE="${{ github.event.pull_request.base.sha }}..${{ github.event.pull_request.head.sha }}"
          elif [[ "${{ github.event.before }}" == "0000000000000000000000000000000000000000" ]]; then
            RANGE="HEAD"
          else
            RANGE="${{ github.event.before }}..${{ github.sha }}"
          fi
          python3 scripts/repo-hygiene-gate.py --mode push --range "$RANGE"
