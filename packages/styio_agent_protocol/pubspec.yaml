name: styio_agent_protocol
description: Pure, versioned protocol types shared by Styio IDE and compatible coding agents.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.11.5 <4.0.0"

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
